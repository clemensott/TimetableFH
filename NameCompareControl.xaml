<UserControl
    x:Class="TimtableFH.NameCompareControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TimtableFH"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <AutoSuggestBox Grid.Column="0" Name="asbName" Margin="3"
                        Text="{Binding Name,Mode=TwoWay}" GotFocus="AutoSuggestBox_GotFocus"/>
        <local:CompareTypeComboBox Grid.Column="1" SelectedItem="{Binding CompareType,Mode=TwoWay}" Margin="3"/>
    </Grid>
</UserControl>
